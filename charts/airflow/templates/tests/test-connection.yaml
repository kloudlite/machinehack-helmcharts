apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "air-flow.fullname" . }}-test-connection"
  labels:
    {{- include "air-flow.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "air-flow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
