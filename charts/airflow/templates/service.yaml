apiVersion: v1
kind: Service
metadata:
  name: {{ include "air-flow.fullname" . }}
  labels:
    {{- include "air-flow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "air-flow.selectorLabels" . | nindent 4 }}
