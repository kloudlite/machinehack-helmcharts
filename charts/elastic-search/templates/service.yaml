apiVersion: v1
kind: Service
metadata:
  name: {{ include "elastic-search.fullname" . }}
  labels:
    {{- include "elastic-search.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "elastic-search.selectorLabels" . | nindent 4 }}
