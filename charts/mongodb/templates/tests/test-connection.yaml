apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mongobd.fullname" . }}-test-connection"
  labels:
    {{- include "mongobd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mongobd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
